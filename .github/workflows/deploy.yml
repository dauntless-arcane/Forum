# name: Auto Deploy

# on:
#   push:
#     branches: [ "main" ]

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4

#     # 1. Login to Docker Hub
#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}

#     # 2. Build and Push Image
#     - name: Build and push Docker image
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         push: true
#         tags: ${{ secrets.DOCKERHUB_USERNAME }}/forum-backend:latest

#     # 3. Deploy to Server via SSH
#     - name: Deploy to Server
#       uses: appleboy/ssh-action@v1.0.3
#       with:
#         host: ${{ secrets.SERVER_HOST }}
#         username: ${{ secrets.SERVER_USER }}
#         key: ${{ secrets.SSH_PRIVATE_KEY }}
#         script: |
#           # Create directory
#           mkdir -p ~/forum-backend
#           cd ~/forum-backend

#           # Stop existing containers
#           docker-compose down || true

#           # Pull latest image
#           docker pull ${{ secrets.DOCKERHUB_USERNAME }}/forum-backend:latest
          
#     # 4. Transfer .env and docker-compose.prod.yml
#     - name: Copy Config Files
#       uses: appleboy/scp-action@v0.1.7
#       with:
#         host: ${{ secrets.SERVER_HOST }}
#         username: ${{ secrets.SERVER_USER }}
#         key: ${{ secrets.SSH_PRIVATE_KEY }}
#         source: "docker-compose.prod.yml"
#         target: "~/forum-backend/docker-compose.yml"

#     - name: Create .env on Server
#       uses: appleboy/ssh-action@v1.0.3
#       with:
#         host: ${{ secrets.SERVER_HOST }}
#         username: ${{ secrets.SERVER_USER }}
#         key: ${{ secrets.SSH_PRIVATE_KEY }}
#         script: |
#           cd ~/forum-backend
#           # Create .env file from GitHub Secret
#           echo "${{ secrets.ENV_FILE_CONTENT }}" > .env
#           echo "DOCKER_IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/forum-backend:latest" >> .env
          
#           # Start services
#           docker-compose up -d --remove-orphans
#           docker image prune -f
